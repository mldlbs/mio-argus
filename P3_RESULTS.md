# P3 结果：结果验证（H3 修正版）

> 复现：`python p3_verifier.py --epochs 25 --v-epochs 15 --tasks 300`

---

## 1. 为什么做这个

P2 证伪了 H3 的原始形式：如果 verifier 必须**重新接地**来判断候选动作对不对，
则 `V(c) ≡ π(c)`（RoI-pool 与点积可交换），验证与选择同构，组合不可能超过单体。

H3 唯一还活着的形式是**结果验证**：

```
V(旧屏幕, 新屏幕) → 进度是否推进？
```

这要求环境暴露**可观察的进度信号**。P3 构造了这样一个环境。

---

## 2. 环境

在 P0 的环境上加两样东西：

| 元素 | 作用 |
|------|------|
| 屏幕顶部**进度条** | 只有正确动作才推进 |
| 错误动作的**红框标记** | 错误动作也会改变屏幕，但进度条不动 |

关键设计：**"屏幕变了" ≠ "进度推进了"**。
两种动作都会改变屏幕，只有进度条能区分——否则任务就退化成"检测有无变化"，
验证会变得平凡。

另有一条重要的实现约束（P1 已踩过同样的坑）：
**几何与状态必须分离**。元素 bbox 的 jitter 只由 `geom_seed` 决定，
与 progress/wrong/done 无关。否则 before/after 两帧的所有元素位置都在变，
进度信号会被 jitter 噪声淹没。

---

## 3. 三组对照

| 系统 | 说明 |
|------|------|
| **π** | 每步只出 top-1，无验证、无重试 |
| **π + V** | V 判断是否推进；未推进则试 π 的下一个候选（最多 3 次） |
| **π + oracle** | 完美验证器重试（上界） |

成功判据是 **`progress` 真的推进到 `t+1`**，而不是"verifier 接受了"。
（这一点很关键：否则一个恒说接受的 V 能把任务刷满——开发中确实踩过这个坑。）

---

## 4. 结果

设置：π width=32（106,112 参数），V width=8（**6,393 参数，比 π 小 17×**），
4000 训练样本，300 任务/配置。

单步准确率：π = 0.9730，V = **1.0000**（tpr=1.00, tnr=1.00）

| ρ | H | π | π + V | π + oracle | **V 增益** |
|---|---|-----|-------|------------|-----------|
| 0.3 | 1 | 0.9800 | 0.9900 | 0.9900 | **+0.0100** |
| 0.3 | 2 | 0.9367 | 0.9767 | 0.9767 | **+0.0400** |
| 0.3 | 4 | 0.8367 | 0.9000 | 0.9000 | **+0.0633** |
| 0.3 | 8 | 0.5567 | 0.7033 | 0.7033 | **+0.1467** |
| 1.0 | 1 | 0.9767 | 0.9967 | 0.9967 | **+0.0200** |
| 1.0 | 2 | 0.9100 | 0.9800 | 0.9800 | **+0.0700** |
| 1.0 | 4 | 0.8367 | 0.9733 | 0.9733 | **+0.1367** |
| 1.0 | 8 | 0.8033 | 0.9467 | 0.9467 | **+0.1433** |

---

## 5. 结论

1. **H3（结果验证形式）成立**：一个比策略小 17 倍的验证器带来稳定增益。
2. **增益随 H 单调上升**：
   - ρ=0.3：`0.010 → 0.040 → 0.063 → 0.147`
   - ρ=1.0：`0.020 → 0.070 → 0.137 → 0.143`
   这正是预测的方向——每步的失误被复利放大，早期纠正的价值随 H 增长。
3. **`π+V` 与 `π+oracle` 完全相等**，因为 V 在该任务上是完美的（acc=1.0）。
   也就是说，**增益的上界已经被拿到**。
4. 与 P2 的对比给出了 H3 的准确边界：

   | verifier 类型 | 是否成立 | 原因 |
   |---------------|----------|------|
   | 重新接地 `V(c)` | ❌ | 与 π 同构 |
   | 结果验证 `V(旧,新)` | ✅ | 使用了 π 没有的信息：动作的**后果** |

   **H3 的本质不是"验证比选择简单"，而是"验证能否用到策略拿不到的信息"。**

---

## 6. 局限

- 进度信号是**合成的、无噪声的**。真实 UI 的进度信号（页面跳转、状态变化）
  有噪声、有延迟、有时不可逆，V 的准确率会显著下降，增益会缩小。
- V 的训练在本项目中出现过**偶发崩溃**（三次完整运行中有一次 V 退化为恒输出
  正类，导致 `π+V=0`）。生产使用需要稳定性修复（如类别平衡采样、更长训练、
  或对 logits 做校准）。
- 环境仍是合成的，只有"点击"一种动作。
- 这是**等参数族内**的结果；主假设中"小模型 vs 10¹⁰ 级大模型"的核心比较**仍未测**
  （无 API key，见 [P2_RESULTS.md](P2_RESULTS.md) 第 0 节）。
- 结论依赖环境暴露进度信号。**若环境没有可观察的进度/结果信号，H3 不适用。**
