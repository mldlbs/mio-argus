# MioArgus 研究假设

> **状态**：草案 v0.1
> **日期**：2026-09-17
> **目的**：在写任何训练代码之前，把"为什么一个小模型可以比大模型更适合 Computer Use"钉成可证伪的命题。

---

## 0. 一句话

**MioArgus 的目标不是让 3B 模型"拥有"70B 模型的知识，而是让一个窄域专用策略在 Computer Use 任务空间里，以更低算力达到甚至超过大模型的有效表现。**

对照组成不是 foundation model，而是 **AlphaZero / AlphaDev 这类"用自生成环境打败规模"的专用策略**。

---

## 1. 问题陈述

当前 MioArgus 走的是：

```
更多截图 → 更大模型 → 更好结果
```

这条路容易陷入资源竞争，且在 CUA 上未必成立，因为 CUA 有结构性性质：

1. **屏幕本身是外部记忆** —— 信息可以重新获取，不必压进参数
2. **有效动作空间很小** —— 给定一个 UI，可操作元素 E_t 通常只有几十个，而不是 1920×1080 个像素
3. **验证比生成容易** —— "这步成功了吗"是二分类，比"该做哪一步"简单
4. **任务是程序性知识** —— 熵远低于自然图像/开放文本

如果这些性质成立，那"小而强"就不是玄学，而是这几个性质的直接推论。

---

## 2. 符号与定义

| 符号 | 含义 |
|------|------|
| `π_θ` | 策略，参数 θ，观察 o_t → 动作 a_t |
| `o_t` | 观察 = 截图 + 指令 + 历史 |
| `H` | 任务步长（horizon） |
| `S(θ, H)` | 在步长为 H 的任务集上的成功率 |
| `F(θ)` | 单步推理 FLOPs |
| `C(θ, H)` | 端到端成本 ≈ `F(θ) × steps(H)` |
| `E_t` | 当前屏幕的可操作元素集合，`|E_t| = k` |
| `K_θ` | 存在参数里的知识 |
| `K_env` | 存在环境里、可重新观察的知识 |

---

## 3. 核心假设 H

> **在可控的闭环 GUI 任务集上，存在参数量 ~10⁷–10⁸ 的接地策略 θ_small，使得对足够长的任务：**
>
> **(a)** `S(θ_small, H) ≥ S(θ_large, H)`，其中 `|θ_large| ≈ 10¹⁰`
> **(b)** `C(θ_small, H) ≤ C(θ_large, H)`
> **(c)** 比值 `S(θ_small, H) / S(θ_large, H)` **随 H 单调上升**

### 为什么 (c) 是核心信号

```
success_small / success_large
   ^
   |                          ____  ← 闭环 + 验证开始占优
   |                        /
   |                      /
   |____________________/
   +---------------------------> H
        crossing point H*
```

- **假设存活** ⇔ 存在有限 H\*，且越过 H\* 后小模型占优
- **假设死亡** ⇔ 大模型在所有 H 上均匀更好（曲线平坦）

**要证明的不是"小模型也很准"，而是这条 crossing 曲线。**

### 机制解释

长任务里误差会累积。

- **大模型（开环）**：知识在参数里，错了无法自纠，只能靠更强的先验避免错误
- **小模型（闭环）**：可以 重新观察 → 验证 → 重试，用环境替代先验

所以长任务应该放大闭环优势，而不该放大规模优势。

---

## 4. 子假设（可独立测试）

### H1 — 外部化
> 重新观察可以替代参数化世界知识。

- **替代关系**：环境 ↔ 参数
- **测法**：固定目标成功率，扫"每步重观察次数"，测所需 `|θ|`
- **预测**：重观察越多，达到同成功率所需 `|θ|` 越小

### H2 — 动作压缩 ★（最先做）
> 在元素集合上做选择，比在像素空间回归便宜一个量级。

- **替代关系**：结构 ↔ 感知容量
- **测法**：同一个小模型，对比
  - `pixels → (x, y) ∈ R²`  (坐标回归)
  - `pixels → element_id ∈ E_t` (分类，熵 = log k)
- **预测**：element_id 版本用**少得多的参数**达到同成功率
- **为什么先做**：最便宜、最定性、不需要训练大模型

### H3 — 验证替代精度 ⚠️（拆成两种形式，结论相反）
> **重新接地**的 verifier ❌ 不成立；**结果验证** ✅ 成立。

- **形式 A（重新接地）** `V(候选动作) → 对不对？`：**证伪**（见 [P2_RESULTS.md](P2_RESULTS.md)）
  - 原理：`π logit(c) = <q, f_c> = V score(c)`（RoI-pool 与点积可交换）→ 同构，不可能超过 π
  - 实证：共享权重时分数最大绝对差 0.007；策略弱时 V 使成功率 0.86→0.60
- **形式 B（结果验证）** `V(旧屏幕, 新屏幕) → 状态是否推进？`：**成立**（见 [P3_RESULTS.md](P3_RESULTS.md)）
  - 需要环境暴露**可观察的进度信号**
  - 实证：V 仅 6,393 参数（比 π 小 17×），增益随 H 单调上升
    （ρ=1.0：`+0.020 → +0.070 → +0.137 → +0.143`），且已达到 oracle 上界
- **H3 的本质**：不是"验证比选择简单"，而是**"验证能否用到策略拿不到的信息"**
  （动作的后果 vs 动作本身）。
- **适用边界**：环境无进度/结果信号时，H3 不适用。

### H4 — 窄域低熵
> UI 输入的熵远低于自然图像，所以达到同等 CUA 能力所需参数更少。

- **替代关系**：任务熵 ↔ 参数量
- **测法**：同架构扫参数量，看是否**过早饱和**
- **预测**：CUA 的 scaling curve 比通用 VLM 更早饱和

---

## 5. 定位与对照组

| | MioArgus | 通用 VLM |
|---|----------|----------|
| 目标 | 窄域 CUA 成功率 | 通用能力 |
| 对照 | AlphaZero / AlphaDev / 专用 RL policy | GPT-4V / Claude |
| 数据 | 自生成环境 + 自博弈 | 海量互联网数据 |
| 评测 | 任务套件 | 公开 benchmark |
| 架构 | 状态编码 + 策略 + verifier | 单体大模型 |

**这不是"我们的 VLM 比你们的 VLM 强"，而是"专用策略在窄域打败规模"。**

---

## 6. 实验协议：H-sweep + 结构消融

### 环境要求（生死线）

环境必须让**记忆与接地成为瓶颈**，否则大模型一眼做完，假设必然被错误证伪。必须满足：

- [ ] **长步长**（误差会累积）
- [ ] **大量相似元素**（grounding 成为瓶颈）
- [ ] **状态会变、必须追踪**
- [ ] **需要错误恢复**（弹窗遮挡、页面变化、滚动后坐标变化）
- [ ] **可控**（可程序化生成任务、有 ground truth）

候选：自建合成 Web App（已知 DOM）→ MiniWoB++ → WebArena-lite

### 四个配置（同一套任务、同一个策略头，只换输入接口）

| 代号 | 配置 | 检验 |
|------|------|------|
| **A** | 大 VLM 零/少样本（API） | 基线 |
| **B** | 小模型：pixels → action | — |
| **C** | 小模型：pixels → 结构状态 → action | H1, H2 |
| **D** | C + verifier | H3 |

### 扫参

`H ∈ {1, 3, 5, 10, 20}`

### 预测

```
C > B              结构化有用          (H2)
D > C              验证有用            (H3)
(D − A) 随 H 增大   ← 核心信号          (H)
```

---

## 7. 消融矩阵

| 维度 | 开启 | 关闭 | 检验 |
|------|------|------|------|
| 结构化状态 | a11y/DOM/element 列表 | 原始像素 | H2 |
| 动作空间 | element_id 分类 | 坐标回归 | H2 |
| 闭环 | 每步重新观察 | 一次观察出全序列 | H1 |
| 验证 | verifier gate | 无 | H3 |
| 参数量 | 扫 1M → 100M | — | H4 |

---

## 8. 指标

| 指标 | 说明 |
|------|------|
| **成功率** | 主指标，按 H 分层报告 |
| **端到端 FLOPs** | `F × steps`，**不是单次调用** |
| **参数量** | 总参数 / 可训练参数（分开报） |
| **纠正率** | 失败后重试成功的比例（闭环能力的直接证据） |
| **crossing H\*** | 小模型开始占优的步长（核心结论） |

---

## 9. 证伪 / 杀死条件

假设死亡，当且仅当：

1. 大模型在所有 H 上均匀更好，且 `S/H` 曲线无交叉
2. H2 不成立：element_id 分类不比坐标回归更省参数（结构无价值）
3. H3 不成立：`(π+V)` 不优于等算力单体（验证无价值）
4. 结构化状态必须用一个和大模型同量级的解析器才能得到（参数只是搬家）
5. 闭环步数成本抵消了精度收益（`C` 反而更高）

**任何一条成立，就要重新审视整个路线，而不是继续加数据。**

---

## 10. 风险

| 风险 | 说明 | 缓解 |
|------|------|------|
| **解析器本身很贵** | 只是把参数挪了位置 | 用廉价的 UI 解析（OCR + 元素检测 / a11y tree） |
| **verifier 和策略一样难** | H3 可能是空的 | 先用可程序化验证的环境测 |
| **环境太窄** | 合成 100% ≠ 真实迁移（v2 已踩过） | 至少接一个真实 Web 环境 |
| **闭环成本** | 步数变多，延迟上升 | 必须算端到端成本 |
| **依赖 a11y tree** | 那就不算在做视觉 | 作为显式设计决策，早做选择 |
| **合成数据自欺** | 之前的 100% 是假的 | 主指标必须是**真实/半真实环境**上的成功率 |

---

## 11. 当前架构与假设的差距

**这是一个必须承认的事实：现有 MioArgus 与"小而强"的假设方向相反。**

| 现状 | 问题 |
|------|------|
| ViT-B/16 (86M) + GPT-2 (124M) = **220M 冻结** | 既不大也不小 |
| backbone 冻结，仅叠 LoRA (r=16) | 通用互联网特征从未为 UI 训练过，无法特化 |
| `pixels → (x, y)` 坐标回归 | 正是 H2 预测应该避免的形态 |
| 无 verifier，开环单步 | H1/H3 完全没有利用 |
| 100% 来自合成数据 | 你自己的记录已标注"不代表真实性能" |

**结论**：如果目标是验证本假设，现有架构不应作为起点。它适合作为"Bet B 的朴素基线"（配置 B），用来对照配置 C 是否更好。

---

## 12. 分阶段里程碑

| 阶段 | 内容 | 产出 | 判据 |
|------|------|------|------|
| **P0** ✅ | 可控环境 + H2 单点验证 | `p0_h2.py` → [P0_RESULTS.md](P0_RESULTS.md) | **通过**：element_id 显著更省参数 |
| **P0.5** ✅ | 把 oracle 换成廉价 CV 解析器 | `p0_select.py` → [P0_5_RESULTS.md](P0_5_RESULTS.md) | **通过**：recall > 0.5 时优势仍在 |
| **P1** ✅ | H1 重观察 vs 参数（horizon 扫描） | `p1_horizon.py` → [P1_RESULTS.md](P1_RESULTS.md) | **通过**：gap 随 H 单调上升 |
| **P2** ⚠️ | H3 verifier + 真实大模型基线 A | [P2_RESULTS.md](P2_RESULTS.md) | **H3(重新接地) 证伪**；配置 A 未跑（无 API key） |
| **P3** ✅ | 结果验证（修正版 H3） | `p3_verifier.py` → [P3_RESULTS.md](P3_RESULTS.md) | **通过**：小 17× 的 V，增益随 H 单调上升 |
| **P4** | 真实/半真实环境迁移 | 迁移后的曲线 | crossing 仍存在 |

**P2 结果（2026-09-17）**：
- **H3 在"重新接地的 verifier"形式下证伪**：`V(c) ≡ π(c)`（RoI-pool 与点积可交换），
  验证与选择同构，组合不可能超过单体。实证：共享权重时分数最大差 0.007；
  策略弱时 V 使成功率 0.86→0.60，策略强时双双饱和。
- **配置 A（真实大模型基线）未跑**：环境无 LLM API key。适配器已留
  （`vlm_adapter.py`，支持 OpenAI/Anthropic、coord/mark 两种模式）。
  **主假设中"小模型 vs 10¹⁰ 模型"的核心比较仍未测。**

**P3 结果（2026-09-17）**：
- 环境加了**进度条**（正确动作才推进）与错误标记；**几何与状态分离**
  （jitter 由 geom_seed 决定），否则进度信号会被渲染噪声淹没。
- 成功判据用 `progress`，不用"verifier 接受了"——否则恒说接受的 V 能刷满任务。
- 结果：V 仅 **6,393 参数（比 π 小 17×）**，acc=1.0；
  **增益随 H 单调上升**（ρ=1.0：`+0.020→+0.070→+0.137→+0.143`），已达 oracle 上界。
- 与 P2 合并给出 H3 的边界：**重新接地 ❌ / 结果验证 ✅**。
  H3 的本质是**验证能否用到策略拿不到的信息**（动作的后果）。
- 局限：进度信号是合成无噪声的；V 训练出现过偶发崩溃（3 次运行中 1 次）。

**P0 结果（修正后，2026-09-17）**：
- 主要难度轴是**精度要求（元素尺寸）**，不是元素数量：
  fill 0.75→0.28 时 cls 恒 1.00，reg/coord 从 1.00 掉到 0.38，Δ 单调 0.003→0.622。
- 64 元素时 Δ 达 +0.93；12K 参数的 cls ≈100%，392K 的 reg 仅 ~10%。
- ⚠️ 初版结论因两个缺陷被高估（缓存 key 缺 `fill`、`gauss` 监督实现偏弱）。
  修正过程与证据见 P0_RESULTS.md 的「修正说明」。

**P0.5 结果（2026-09-17）**：
- 用**不含任何学习模型**的经典 CV 解析器（阈值 + 开运算 + 连通域）。
- `cls_hit ≈ parser recall`：瓶颈是**召回**，不是选择能力。
- 抖动几乎无害（IoU 0.72→0.50，cls 仍 99.95%）。
- **crossing 在 recall ≈ 0.5**：Q8（recall 0.498）时 parser-cls 0.472 首次低于 reg 0.494。
- 工程目标：**目标元素解析召回 > 0.5**，框精度要求宽松得多。
- 仍是合成环境；真实 UI 的解析器召回会远低于 clean 档。

**P1 结果（2026-09-17）**：
- 设计：H 步任务，布局每步以概率 ρ 重随机，观察间隔 k（`k=1` 闭环，`k=H` 开环）。
- **Sanity check**：ρ=0 时 k=1/2/4/8 结果**逐位相同** → 环境不变时重观察价值为零。
- **有重观察时小模型撑得住长 horizon**：ρ=1、H=8、k=1 仍达 **0.9967**。
- **无重观察时迅速归零**：ρ=1、k≥2、H≥2 → **0.0000**。
- **重观察价值随 H 单调增长**（ρ=0.3：gap 0.00→0.29→0.65→0.92）。
- **两个机制可分离**：reg 的崩塌与 k 无关（ρ=0 时各 k 相同），
  是**误差复利** p^H（0.53→0.007），不是观察缺失；cls 的崩塌才是观察缺失。
- 局限：同参数量比较，**没有大模型基线**；布局是独立重随机而非小幅演化；仍为合成环境。


**P0 是最便宜、最能定性的一刀。H2 不成立，后面全部不用做。**

---

## 附：为什么值得一试（先例）

| 先例 | 结论 |
|------|------|
| AlphaZero | 无人类数据、自博弈、小网络 → 超越人类与暴力搜索 |
| AlphaDev | 专用策略找到比人类更好的排序算法 |
| 窄域 RL policy | 在受限任务空间内，专用 > 通用规模 |

MioArgus 的赌注是：**CUA 也属于这类窄域。**

---

*本文件是活的。任何实验一旦给出反例，直接修改对应假设，而不是修改解释。*
