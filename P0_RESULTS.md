# P0 结果：H2 动作空间压缩

> 自动生成自 `p0_h2_results.json` / `p0_regloss_results.json`。不要手改。

```
python p0_h2.py --epochs 25 --seeds 2
python p0_regloss.py --epochs 25 --seeds 2
python p0_report.py
```

## ⚠️ 修正说明（必须读）

本文件曾有一版**错误结论**，原因是两个真实缺陷：

1. **缓存 key 未包含 `fill`**：加入元素尺寸扫描后，所有 fill 配置
   都复用了最早按 `fill=0.70` 构建的缓存 → 扫描 A 实际上只有一份数据。
2. **`gauss` 监督实现有缺陷**：它系统性弱于坐标 MSE。
   原扫描 B 用的是 `reg/gauss`，等于拿一个被削弱的 reg 基线去比，
   **高估了 Δ**（grid=6 时报 0.07，强基线实为 0.50）。

现在：缓存 key 覆盖全部 env 参数；reg 基线一律用**较强的 `coord` 损失**。
下表为修正后结果。

## 设置

| 项 | 值 |
|----|----|
| device | cuda |
| train / test | 4000 / 1000 |
| epochs / seeds | 25 / 2 |
| backbone | 3 层小 CNN，保留空间结构 |
| 动作头 | reg=soft-argmax 坐标（coord 损失）；cls=g×g 池化分类 |
| 参数量 | **两头完全相同** |
| 判据 | 执行点是否落在目标元素 bbox 内 |

## 扫描 A：精度要求（元素尺寸，grid=6）★ 主要难度轴

| 配置 | precision | cls hit | reg hit | Δ |
|------|-----------|---------|---------|---|
| fill=0.75 | 0.125 | 1.0000 | 0.9970 | +0.0030 |
| fill=0.55 | 0.0917 | 0.9995 | 0.8085 | +0.1910 |
| fill=0.4 | 0.0667 | 1.0000 | 0.5110 | +0.4890 |
| fill=0.28 | 0.0467 | 1.0000 | 0.3785 | +0.6215 |

## 扫描 B：格子数（元素数量，fill=0.40）

| 配置 | slots | cls hit | reg hit | Δhit | reg cell |
|------|-------|---------|---------|------|----------|
| grid=3 | 9 | 1.0000 | 1.0000 | +0.0000 | 1.0000 |
| grid=4 | 16 | 1.0000 | 0.9605 | +0.0395 | 0.9990 |
| grid=5 | 25 | 1.0000 | 0.5025 | +0.4975 | 0.9955 |
| grid=6 | 36 | 0.9995 | 0.4050 | +0.5945 | 0.9535 |
| grid=8 | 64 | 1.0000 | 0.0480 | +0.9520 | 0.2950 |

## 扫描 C：参数量（grid=8, fill=0.28）

| width | params | cls hit | reg(coord) | reg(gauss) |
|-------|--------|---------|------------|------------|
| 8 | 12,320 | 0.9980 | 0.0550 | 0.0035 |
| 16 | 32,064 | 0.9995 | 0.0720 | 0.0025 |
| 32 | 106,112 | 1.0000 | 0.0780 | 0.0045 |
| 64 | 392,448 | 1.0000 | 0.1060 | 0.0025 |

## reg 损失消融（诚信核查）

| grid | slots | cls | reg coord | reg gauss | Δ(coord) | Δ(gauss) |
|------|-------|-----|-----------|-----------|----------|----------|
| 3 | 9 | 1.0000 | 1.0000 | 0.6530 | +0.0000 | +0.3470 |
| 4 | 16 | 1.0000 | 0.9620 | 0.3195 | +0.0380 | +0.6805 |
| 5 | 25 | 1.0000 | 0.5170 | 0.0250 | +0.4830 | +0.9750 |
| 6 | 36 | 1.0000 | 0.5035 | 0.0045 | +0.4965 | +0.9955 |
| 8 | 64 | 0.9995 | 0.0700 | 0.0045 | +0.9295 | +0.9950 |

`gauss` 全面弱于 `coord`（grid=3 时 0.65 vs 1.00）。
这解释并修正了初版结论的高估。

## 结论（修正后）

1. **cls 在所有难度下 ≈100%**，12K 参数即足够。
2. **主要难度轴是精度要求（元素尺寸），不是元素数量**：
   fill 从 0.75 → 0.28，cls 恒 1.00，而 reg/coord 从 1.00 掉到 0.38，
   Δ 单调 0.003 → 0.622。
3. **元素数量同样有效**：grid 8→64 元素时 Δ 达 +0.93。
4. **参数量换不回**：grid=8 时 12K 的 cls ≈100%，392K 的 reg 仅 ~10%。
5. reg 的 cell_acc 通常仍较高（0.95–1.00），说明它**找到了位置却不够准**；
   只有在 grid=8 时 cell_acc 才崩到 0.30——粗定位也开始失效。

## 这**不**意味着什么

- 结构化接口在 P0 用 **oracle 元素 bbox**，这是结构化的能力**上界**。
- 环境是合成的，未验证真实 UI 迁移。
- 只测了 H2，未测 H1/H3/H4，也未测主假设的 horizon-crossing。
- 指令 → 颜色的映射是语言接地的代理。
- gauss 监督的失败是本实现的问题，不代表热力图监督本身不行。
