# P4c-OOD 结果：P4a' 的优势**不可迁移**

> 复现：`python p4c_ood.py`
> 设计：10 个 domain 置换，训练只用 0–5，留 6–9 从未见过。

---

## 1. 结果

```
chance = 1/36 = 0.0278
```

| | seen domain | **OOD domain** | drop |
|---|---|---|---|
| best generalist | 0.3183 | **0.0425** | +0.2758 |
| population + routed | **0.7208** | **0.0583** | **+0.6625** |
| oracle（上界） | 0.7483 | 0.1950 | +0.5533 |
| router acc | 1.0000 | **0.0000** | — |

**population 对通才的优势：seen +0.4025 → OOD +0.0158。**

双方在 OOD 上都塌到随机（0.028）附近。

---

## 2. 解读

### P4a' 的优势是**查表记忆**，不是可迁移能力

在 seen domain 上，专精者各自记住了**自己那一张** label→color 表；
通才被容量限制，装不下 6 张。这是**记忆容量**的胜负，不是能力的胜负。

一旦 domain 未见，专精者手里那张表完全无效 → 掉到随机。
通才同样没学过 → 也掉到随机。

**population 的优势完全依赖于"该 domain 有专精者"，也就是依赖于域覆盖。**
未见域没有专精者，优势自然消失。

### router 在 OOD 上是 0.0000——这不是 bug，是结构性限制

router 的输出空间只有训练过的 6 个域。未见域**根本无法被表达**。
所以：

> **MoE + router 的前提是"每个域都有一个专家"。你无法路由到一个你没见过的域。**

---

## 3. 自我批评：P4a' 是一个被污染的实验

回看 P4a'，公平的结论应该更弱：

| P4a' 我以为在测 | 实际在测 |
|---|---|
| 专精能否产生更强的能力 | **D 张小表 vs 1 张装不下的小表** |
| population 的价值 | 记忆容量的分配 |

**我构造的冲突（任意颜色置换）恰好是最不可泛化的那种**——
它是纯查表，没有任何可学习的结构。在这种任务上，
"多个专家各记一张表"当然赢过"一个专家硬记 D 张"。
这不是能力结果，是容量结果。

OOD 实验把这个混淆暴露了出来。**这正是做 OOD 的意义。**

---

## 4. 对路线的影响

### population 路线**未被证实**，且 P4a' 不再能作为支撑

| 结论 | 状态 |
|------|------|
| P4a（通才可覆盖）→ population 无增益 | ✅ 仍然成立 |
| P4a'（通才被卡住）→ population +0.36 | ⚠️ **是记忆容量结果，OOD 不迁移，不能作为能力证据** |
| population 产生可迁移能力 | ❌ **未观察到** |

### P4a' 唯一还站得住的部分

它证明了一件**平凡但有用**的事：

> 当任务空间由若干个**互不相关**的子域组成、且单一策略在预算内无法覆盖时，
> **MoE + 路由器** 优于等参数单体模型。

这是已知结论，**不构成研究贡献**。

---

## 5. 要公平地检验 population，需要什么

必须让 domain 之间**可学习地相关**，即：未见域可以从屏幕上的某个**可观察线索**推断出来。例如：

```
domain d  →  颜色 = base[label] 按 d 旋转，且屏幕顶部有一条指示条显示 d
```

这样：
- **通才**：可以学会"读指示条 → 应用旋转"这个**算子**，从而泛化到未见 d
- **专精者**：各自记住一个 d，遇到未见 d 必失败
- **router**：无法路由到不存在的专家

**预测：这种情况下 population 会在 OOD 上输给通才。**

如果这个预测成立，那么结论是反的：**专精损害泛化，population 路线应当放弃。**

如果通才也学不会这个算子（小模型能力不足），则实验无信息量——
需要换更小的域集合或更强的模型。

---

## 6. 结论

1. **P4a' 的优势不可迁移**：OOD 上 population 0.0583 vs 通才 0.0425（差 0.016）。
2. **根因**：我构造的 domain 冲突是纯查表，不可泛化；实验测的是记忆容量。
3. **population 路线目前没有实证支撑**。P4a 说它无效，P4a' 说它有效但
   被 OOD 证伪为记忆效应。
4. **下一步（P4d，可选）**：把 domain 改成**可观察参数 + 可学习算子**，
   公平检验"专精 vs 泛化"。预计 population 会输；若输，则正式放弃该路线，
   回到 H1/H2/H3 的单模型路线。

---

## 7. 局限

- OOD 任务按构造是**不可解的**（未见置换在屏幕上无任何线索），
  所以它证明的是"没有域覆盖就没有能力"，而不是"population 一定不能泛化"。
- 环境是合成的、单动作类型。
- 未测"可观察参数 + 可学习算子"的版本（见 §5，即 P4d）。
- 仍未测主假设中"小模型 vs 10¹⁰ 大模型"的比较（无 API key）。
