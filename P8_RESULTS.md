# P8 结果：学习式 UI 检测器 —— "参数搬家"风险检验

> 复现：
> ```
> python p8_learned_parser.py --n-train 3000 --n-test 1000 --epochs 22
> python p8_learned_parser.py --bgs flat --det-widths 16,32 \
>     --n-train 4000 --n-test 1000 --epochs 60
> ```

---

## 1. 动机

P0.5 埋下的风险：

> **"如果解析器本身要个大模型，那就只是把参数搬了个位置。"**

P7 证实朴素 CV 解析器在纹理背景上崩溃（recall 0.847 → 0.026）。
P8 用一个**小型学习式检测器**（CenterNet-lite）替换它，回答：

> 在**等总参数量**（检测器 + 选择器）下，
> 学习式解析器 + 选择器 能否打败坐标回归（`reg`）？

判据：`total_params < reg_params` **且** `cls > reg`。

---

## 2. 检测器设计

anchor-free，无 NMS 库依赖，可随 width 缩放：

```
backbone → 特征图 (C, 24, 24)          # 192px / 8
objectness : 1×1 conv → sigmoid        目标 = 元素中心的 Gaussian (sigma=1 cell)
size       : 1×1 conv → 2 通道          目标 = 元素 (w,h) / IMG_SIZE
decode     : max-pool 局部极大 + 阈值 + top-k → 框
```

任务：`tiny` 布局（42 个元素，8–26 px，即 ~9% 图像尺寸）—— 正是 P7 里
`reg` 失效、结构化有优势的区间。

---

## 3. 结果

### 3.1 跨背景（3000 样本 / 22 epochs）

| bg | det_w | det 参数 | sel 参数 | total | recall | IoU | cls | reg |
|----|-------|---------|---------|-------|--------|-----|-----|-----|
| flat | 4 | 2,043 | 30,016 | 31,699 | 0.388 | 0.113 | 0.3750 | 0.7020 |
| flat | 16 | 24,003 | 30,016 | 54,019 | 0.494 | 0.196 | 0.4870 | 0.7020 |
| flat | 32 | 94,083 | 30,016 | 124,099 | 0.527 | 0.220 | 0.5200 | 0.7020 |
| texture | 16 | 24,003 | 30,016 | 54,019 | 0.456 | 0.161 | 0.4290 | 0.6230 |
| texture | 32 | 94,083 | 30,016 | 124,099 | 0.481 | 0.174 | 0.4410 | 0.6230 |
| natural | 16 | 24,003 | 30,016 | 54,019 | 0.437 | 0.161 | 0.3780 | 0.4140 |
| natural | 32 | 94,083 | 30,016 | 124,099 | 0.453 | 0.174 | 0.4080 | 0.4140 |

**12/12 配置全部 `NO`**（`cls < reg`）。

### 3.2 充分训练对照（4000 样本 / 60 epochs，flat）

| det_w | total | recall | IoU | cls | reg |
|-------|-------|--------|-----|-----|-----|
| 16 | 54,019 | **0.518** | 0.213 | 0.5170 | **0.7840** |
| 32 | 124,099 | **0.515** | 0.236 | 0.5110 | 0.7840 |

**recall 在 0.52 处饱和**：容量从 24K → 94K 参数、训练从 22 → 60 epochs，
recall 几乎没有变化（0.494 → 0.518）。

---

## 4. 结论

### 4.1 学习式检测器**无法满足判据**

要求是 `recall > reg_acc`：

| 背景 | 需求（> reg_acc） | 检测器实际 recall | 满足？ |
|------|------------------|------------------|--------|
| flat | > 0.784（60ep） | **0.518** | ❌ |
| texture | > 0.623 | 0.456 | ❌ |
| natural | > 0.414 | 0.437 | ✅（勉强） |

**而且加大容量、延长训练都不改变这个结论**（recall 卡在 ~0.52）。

### 4.2 更反直觉的发现：**经典 CV 解析器在干净背景上打得过小学习检测器**

| 解析器 | flat recall | texture recall |
|--------|------------|----------------|
| **经典 CV（零参数、人工先验）** | **0.847** | 0.026 |
| **学习式检测器（24K–94K 参数）** | 0.518 | **0.456** |

- **干净背景**：人工先验（"元素是均匀背景上的实心色块"）**远胜**学习
  ——0.847 vs 0.518，而且零参数
- **纹理背景**：先验失效（0.026），学习的鲁棒性显现（0.456）
- **但没有一种解析器在任何背景上都超过它自己的 `reg` 基线**

### 4.3 对整体结论的最终影响

P0.5 的警告被证实为**否定性的**：

> **小预算下，解析器达不到 `recall > reg_acc` 这个门槛。**
> 而且这条门槛不是"学习式检测器不够强"——容量翻 4 倍毫无改善——
> 而是**在这类真实 UI 上检测任务本身就比回归任务难**。

补充说明：**在真实世界里这个结论会被 a11y / DOM 树推翻**——
桌面与 Web 应用大多直接暴露结构化树，解析器接近免费且近满召回，
于是条件立刻满足、`cls-oracle` 的行为（1.0）可直接拿到。
但那时问题已经**不是视觉问题**了。

**⇒ 结构化动作空间的实用区间再收紧一步：**

```
{精度要求高} ∩ {解析器可靠}
```

- **a11y / DOM 可用**（多数桌面与 Web 应用）→ 在区间内，且不需要视觉解析
- **只能看像素**（游戏、远程桌面、画布、图像类应用）→ **不在区间内**：
  - 经典 CV：干净背景召回够，纹理背景崩
  - 小学习检测器：跨背景都只能到 ~0.5，够不到门槛

---

## 5. 局限（对检测器公平起见）

- 检测器是**简化的 CenterNet-lite**：无数据增强、目标函数简单、
  特征图 24×24 对 8–26px 的小目标只有 1–3 个 cell。
  更强的检测架构（FPN、更高分辨率头、DETR 风格）可能显著提高 recall，
  **本实验不能排除这一点**。
- 但注意：**加大 width 从 4× 到 16× 无改善**，说明瓶颈不在容量而在
  检测任务本身的分辨率/定位难度。
- 只测了 `tiny` 布局；其他密度未测。
- 仍是合成环境。
- 未测主假设 vs 大模型（无 API key）。

---

## 6. 下一步（优先级已变）

| 优先级 | 事项 | 为什么 |
|--------|------|--------|
| 高 | **高分辨率检测头 / FPN** | 唯一能反驳 §4.3 的方向：recall 是否真被 24×24 特征图卡住 |
| 高 | **a11y / DOM 路径** | 真实世界里这才是"廉价结构"的正确来源，也是最可能落地的路径 |
| 中 | 真实截图 | 仍缺 |
| 低 | 主假设 vs 大模型 | 见 REPORT.md §12 |
