# P4d 结果 + P4 路线最终裁决

> 复现：`python p4d_capacity_scaling.py`
> 前置：[P4A_RESULTS.md](P4A_RESULTS.md) / [P4B_RESULTS.md](P4B_RESULTS.md) / [P4C_RESULTS.md](P4C_RESULTS.md)

---

## 1. P4d：通才容量扫描

问题：P4a' 里通才被卡在 ~0.33，是**容量不足**还是**结构性不能**？
若给它足够参数能追上 0.6887 → population 只是容量变通；若追不上 → 专精有结构优势。

D=6 个互不相关 domain，6000 训练样本，25 epochs：

| width | params | acc | 各 domain |
|-------|--------|-----|-----------|
| 16 | 27,968 | 0.2842 | 0.284 0.352 0.243 0.265 0.272 0.288 |
| 32 | 99,968 | 0.2750 | 0.256 0.241 0.201 0.289 0.320 0.340 |
| 48 | 218,048 | 0.3133 | 0.275 0.342 0.307 0.319 0.296 0.346 |
| 64 | 382,208 | 0.2683 | 0.246 0.286 0.275 0.260 0.228 0.319 |
| 96 | 848,768 | 0.2217 | 0.185 0.216 0.217 0.225 0.238 0.251 |
| 128 | **1,499,648** | **0.1658** | 0.095 0.151 0.238 0.201 0.141 0.178 |

**加参数不仅没用，反而更差**（过拟合）。

大通才 + 4× 数据（24000 样本）：

| width | params | acc |
|-------|--------|-----|
| 32 | 99,968 | 0.3700 |
| 64 | 382,208 | 0.3708 |
| 128 | 1,499,648 | 0.2892 |

**最强通才 0.3708 @ 382,208 参数（population 的 3.5×），仍未追上 0.6887。**

表面对结论：专精有**结构性**优势，不只是容量变通。

---

## 2. 但真正的原因不是容量 —— 是**可识别性**

通才的输入只有（屏幕, 指令）。它**无法推断当前是哪个 domain**：
domain 只体现在 `label→color` 的映射上，而模型不会读文字。

**所以这个任务对通才在信息上就是不可解的**——不是学不会，是没有足够信息。

这也解释了为什么加参数没用甚至更差：参数再多也补不上缺失的输入信息。

---

## 3. 这使 P4a' 的比较**被污染**

| P4a' 我以为在测 | 实际在测 |
|---|---|
| 专精 vs 泛化 | **有域信息 vs 无域信息** |

专精者因为只训练一个 domain，**隐式获得了域身份**（映射对它是固定的）；
通才必须同时处理 6 张映射，却不知道当前是哪一张。

**这不是一个关于 specialization 的实验，是一个关于 information 的实验。**

---

## 4. 补救尝试（P4e）——未完成

正确做法是让 domain **可观察**：屏幕顶部加一条颜色编码 domain 的指示条，
使通才也能看到域身份。实现时遇到渲染不一致问题
（元素几何与网格线/画布尺寸的交互），标定后专精者准确率仍远低于 P4a' 基线
（0.31 vs 0.75），未能排除是我自己的渲染 bug 还是任务本身的变化。

**该脚本已移除，不留在仓库里。** 这个缺口如实记录在此。

即便 P4e 跑通，它也只能回答"通才在**获得域信息后**能否覆盖"，
而不能挽回 P4a' 的原始比较——那个比较已经被证明是信息不对称，不是专精优势。

---

## 5. P4 路线最终裁决

| 实验 | 表面结论 | 实际结论 |
|------|----------|----------|
| P4a | 通才可覆盖 → population 无增益 | 成立 |
| P4a' | 通才被卡住 → population +0.36 | **被信息不对称污染** |
| P4c | — | OOD 不迁移（+0.40 → +0.02），是记忆效应 |
| P4d | 通才 3.5× 参数仍追不上 | 原因是**域不可识别**，非容量 |

**⇒ P4（population self-evolution）没有建立起任何能力层面的主张。**

它能站住的只有一句已知结论：

> 当任务由若干互不相关子域组成、且单一策略无法覆盖时，
> **MoE + 路由器** 优于等参数单体模型。

这是教科书结论，**不构成研究贡献**。

---

## 6. 那么什么留下了

整个项目里**有实测支撑**的仍然只有单模型那条线：

| 子假设 | 状态 | 关键数字 |
|--------|------|----------|
| H1 重观察替代参数 | ✅ | gap 随 H：0.29 → 0.65 → 0.92 |
| H2 动作空间压缩 | ✅ | 12K 参数 cls ≈100% vs 392K 参数 reg ≈10% |
| H3 结果验证 | ✅ | V 小 17×，增益随 H 单调上升，达 oracle 上界 |
| H4 窄域低熵 | 未测 | — |
| 主假设（vs 大模型） | 未测 | 缺 API key |
| population 自进化 | ❌ | 方法学上被污染，无能力证据 |

**可交付的成果是 H1/H2/H3 三个机制，不是 population。**

---

## 7. 如果还要继续 P4，唯一正确的起点

必须先**消除信息不对称**，而不是继续加机制：

1. 在**同一渲染管线**内加入 domain 指示（不要另起渲染器），
   并先用专精者复现 P4a' 基线（≈0.75）验证管线正确
2. 确认通才在**看得到域身份**后能覆盖 → 若是，population 确定为不必要
3. 若通才看到了域仍覆盖不了 → 那时才是真正的容量/分工结论

**判据很硬**：在动手做 P4b（对抗 curriculum）之前，
必须先有一个**信息对称**的对照实验。否则所有后续结论都不可解释。

---

## 8. 局限

- P4e 未完成（见 §4），"通才看到域后能否覆盖"这个问题**未回答**。
- 环境是合成的、单动作类型（点击）。
- 未测主假设中"小模型 vs 10¹⁰ 大模型"的比较（无 API key）。
- 未做 domain 的无监督发现（router 用了域标签），
  但 router 在可见域上 acc=1.0，说明域可从屏幕推断，标签成本低。
