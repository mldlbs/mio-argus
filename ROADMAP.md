# MioArgus 路线图

> 本文是策略文档，不是结果报告。**每一条主张都必须标注证据等级**：
> `[实测]` 本项目跑出来的数字 / `[文献]` 引用但未核实 / `[假设]` 尚未验证。

---

## 0. 一句话

**把钱从"买数据 / 堆参数"转移到"环境、验证器、搜索与进化机制"上。**

---

## 1. 前置约束（先钉死，否则后面全白做）

### C1 — Verifier 是系统天花板，且 population 会放大 hacking `[实测: P2/P3]`

- P2：**重新接地**的 verifier 与策略同构（`V(c) ≡ π(c)`，分数最大差 0.007）→ 零增益
- P3：**结果验证**的 verifier 有效（6,393 参数，acc=1.0，增益随 H 单调上升）
- 推论：verifier **必须基于环境终局状态**，不能是学出来的判别器
- 推论：N 个模型同时优化同一个不完美 verifier，会在少数几代内系统性找到漏洞

**行动项**：只在"终局状态可机械判定"的任务空间内做 population。
（例：文件内容、数据库状态、沙箱内流程完成。反例："帮我整理桌面"。）

### C2 — 难度必须有坐标 `[实测: P0/P1]`

对抗 curriculum 不能靠"更难"这种模糊说法。我们已测出三个可调难度轴：

| 轴 | 参数 | 实测效应 |
|----|------|----------|
| 精度要求 | 元素尺寸 `fill` | 0.75→0.28 使回归从 1.00 掉到 0.38 |
| 步长 | `H` | π 从 0.98（H=1）掉到 0.80（H=8） |
| 环境变化率 | `ρ` | ρ=1 时开环直接归零 |

**行动项**：task proposer 只在这个参数空间里搜索，不发明新的难度定义。

### C3 — 多样性不能靠不同初始化 `[实测: P2]`

共享 grounder 的模型是同构的。10 个同架构不同 seed 的模型会在数代内塌成同一个策略。

多样性只能注入在**模型能真正不同的地方**：

| 维度 | 取值 |
|------|------|
| 观测接口 | 像素 / 结构化元素列表 |
| 动作空间 | 坐标回归 / 元素选择 |
| 重观察频率 | 每步 / 每 k 步 |
| 验证策略 | 有 / 无 outcome verifier |
| **目标（训练数据切片）** | 见 C4 |

### C4 — 目标必须互相冲突 `[假设]`（P4a 检验）

加权标量化 `F = αS − βA − γT − δC + εR` 的问题：权重拍脑袋、非凸前沿取不到 Pareto 点。
**改为"每个成员一个目标"**，但前提是目标**互相冲突**，否则不产生多样性。

### C5 — 蒸馏与多样性互相拆台 `[假设]`

"explore → merge → compress → re-explore" 每轮都会降低多样性。
**修正**：population 本身不压缩，另起一个 student 只吃轨迹/知识，不吃 population 的位置。

---

## 2. 可证伪的核心问题

> **在什么条件下，population 优于单智能体自训练？**

若环境有 ground-truth reward，单智能体 RL 就够了。population 只在两种情况下必要：
reward 稀疏/多目标冲突，或需要探索多样性跳出局部最优。

四面实验（唯一必须做的实验）：

| 组 | 配置 |
|----|------|
| **A** | SFT only |
| **B** | 单模型 self-evolution |
| **C** | N-model population |
| **D** | N-model population + adversarial curriculum |

**判据**：在**从未参与任何训练**的 OOD 分布上
- `C > B` → population 有净效应
- `D > C` → curriculum 有净效应
- **`C ≤ B` → population 路线当场证伪**

---

## 3. P4 分阶段协议

### P4a — 多样性探针 ❌（未通过判据）

**关键修正**：在单步任务上"行为多样性"不可能存在——最优动作唯一。
真正的多样性是**失败剖面的差异**。因此测 **coverage**，不测 action disagreement。

**结果（见 [P4A_RESULTS.md](P4A_RESULTS.md)）**：专精**确实存在**
（`precise` 在 fill=0.28 上 1.0000 / fill=0.75 上 0.7654；`coarse` 相反），
错误集 Jaccard ≈ 0。**但 coverage 增益 ≈ 0**：
最强单成员的 top-2 覆盖已达 1.0000，population 加不出任何东西。

**根因**：这个任务空间**不需要专精**——单个通才在 1/5 参数预算下已饱和
（`mixed` 106K 参数 → 全难度 0.9993）。

**⇒ population 路线在本任务空间不成立，P4b/c 暂停。**

### P4a' — 让"通才覆盖不了"成为可测事实 ✅（通过）

**做法**：加宽任务空间——引入 D 个 domain，每个 domain 有**独立的
label→palette 随机置换**（现实类比：不同 App 用不同配色）。
通才必须同时装下 D 张查找表；专精者只需一张。

**结果（见 [P4B_RESULTS.md](P4B_RESULTS.md)）**：

| | P4a（通才饱和） | P4a'（通才被卡住） |
|---|---|---|
| 通才 acc | 0.9993（1/5 参数预算） | **0.3273**（width 32 / 100K 参数） |
| population | ❌ 无增益 | ✅ **+0.361** |

代表性数字（参数匹配）：

```
population(w12×6) + router, 109K 参数  →  0.6887
单体通才,              100K 参数  →  0.3273
```

**关键洞察**：价值几乎全部来自**选择机制**。
`vote`（盲目集成）= 0.1447（比通才还差）；`oracle`（事后选对）= 0.7227；
`routed`（6,342 参数的小 router）= **0.6887**。
⇒ 印证 C1：**天花板是 selector，不是容量。**

**⇒ population 路线在正确的任务空间里成立，P4b/P4c 解锁。**

### P4b — 对抗 curriculum

| 指标 | 判据 |
|------|------|
| proposer 能否在难度轴上找到击败当前策略的任务 | 成功率下降 ≥ 阈值 |
| 生成任务是否可迁移（不是单点过拟合） | 在相邻难度上也下降 |

### P4c — 四面实验 + OOD

**前置（P4a' 遗留的硬缺口）**：必须做 **OOD domain 测试**——
把 domain 置换参数化（由种子决定），留出**从未见过的置换**。
只有 population 在未见 domain 上仍赢，结论才真正成立。

- A/B/C/D 四组
- OOD 分布：**不同的 label 集 / grid / 难度轴组合 / domain 置换**，且从未参与训练
- 指标：OOD 成功率、多样性、到平台期的代数

---

## 4. 相对已有工作的 delta（待核实）

⚠️ **GroundCUA / SEA / AlphaEvolve 未能独立核实**（本环境无检索能力）。
以下 delta 是**暂定**的，必须先读完这三篇再定稿：

| # | Delta | 证据等级 |
|---|-------|----------|
| 1 | population（而非单模型） | 需核实 SEA 是否单模型 |
| 2 | 多目标特化（而非成功率单目标） | 需核实 |
| 3 | **对抗 curriculum 作用在可度量的难度轴上** | `[实测]` 难度轴由 P0/P1 给出 |
| 4 | 蒸馏到小模型 | 需核实 |

**第 3 条是唯一有本项目实证支撑的差异化点。**

---

## 5. 结构性优势：小模型让进化可行

```
10 × 70B × RL × 环境     ← 不可行
10 × 0.4M × 进化 × 环境  ← 我们现在的规模，可跑数百代
```

我们已在 12K–400K 参数上拥有全部零件（环境 / verifier / 结构化动作 / 地平线任务）。
**population 实验的边际成本接近于零**，这是相对所有 CUA 工作的结构性优势。

---

## 6. 当前证据总览

| 子假设 | 状态 | 关键数字 |
|--------|------|----------|
| H1 重观察替代参数 | ✅ `[实测]` | gap 随 H：0.29→0.65→0.92 |
| H2 动作空间压缩 | ✅ `[实测]` | 12K cls ≈100% vs 392K reg ≈10% |
| H3 验证替代精度 | ⚠️ 拆解 `[实测]` | 重新接地 ❌ / 结果验证 ✅（小 17×，增益随 H 升） |
| H4 窄域低熵 | 未测 | — |
| 主假设（vs 大模型） | **未测** | 缺 API key |

---

## 7. 风险登记

| 风险 | 说明 | 缓解 |
|------|------|------|
| Reward hacking | N 个模型同时找 verifier 漏洞 | verifier 基于环境终局状态（C1） |
| Population collapse | 成员趋同 | 目标冲突（C4）+ 接口差异（C3） |
| Self-generated garbage | 自己生成错误轨迹并自我强化 | 独立 verifier + 环境 ground truth |
| Early winner lock-in | 早熟成员挤死晚熟成员 | 保留多样性压力，不做纯精英复制 |
| 算力 | N × RL × 环境 | 用极小模型（我们的规模） |
| **无法迁移** | 合成环境的结论不代表真实 UI | P4c 必须含 OOD；最终需真实环境 |

---

## 8. 下一步

1. **P4a 多样性探针**（本文档配套实现：`p4_population.py`）
2. 核实 GroundCUA / SEA / AlphaEvolve，定稿第 4 节的 delta
3. 若 P4a 通过 → P4b；不通过 → 回到 C4 重新设计目标
