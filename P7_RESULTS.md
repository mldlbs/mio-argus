# P7 结果：真实自由布局 UI 上的迁移检验

> 复现：
> ```
> python p7_realistic_ui.py --n-train 4000 --n-test 1200 --epochs 25
> ```

---

## 1. 动机

REPORT.md §10 的最大局限：**所有结论都建立在网格状、纯色 UI 上**。
真实 UI 是自由摆放、大小不一、有圆角/边框/阴影/渐变、会重叠、背景有纹理。

本实验去掉网格，做**两因子分解**，把"动作空间优势"与"解析器代价"分开：

| 条件 | 测什么 |
|------|--------|
| `reg` | 像素 → (x,y) |
| `cls-oracle` | 真值框 → 选择 → 执行中心（纯动作空间效应） |
| `cls-parser` | 廉价 CV 解析器框 → 选择 → 执行（加上解析器代价） |

**"廉价"的定义**：不含任何学习模型——四角估计背景 + 阈值 + 形态学开运算 + 连通域。

布局密度（决定**精度要求**）：sparse(12 大) → medium(20) → dense(30) → extreme(42 小)。
背景：flat / texture / natural。

---

## 2. 结果

| config | recall | reg | cls-oracle | cls-parser | **space** | **parser cost** | cls-parser vs reg |
|--------|--------|-----|-----------|-----------|-----------|-----------------|-------------------|
| sparse+flat | 0.905 | 1.0000 | 1.0000 | 0.8967 | +0.000 | +0.103 | **−0.103** |
| medium+flat | 0.863 | 1.0000 | 1.0000 | 0.8550 | +0.000 | +0.145 | **−0.145** |
| dense+flat | 0.848 | 0.9958 | 1.0000 | 0.8342 | +0.004 | +0.166 | **−0.162** |
| extreme+flat | 0.845 | 0.9725 | 1.0000 | 0.8325 | +0.028 | +0.168 | **−0.140** |
| extreme+texture | **0.010** | 0.9533 | 1.0000 | 0.0067 | +0.047 | +0.993 | **−0.947** |
| extreme+natural | **0.095** | 0.9258 | 0.9992 | 0.0708 | +0.073 | +0.928 | **−0.855** |

（space = cls-oracle − reg；parser cost = cls-oracle − cls-parser）

---

## 3. 三条发现

### 3.1 动作空间优势在真实布局上**几乎为零**

| 环境 | space 效应 |
|------|-----------|
| 网格（P0，fill=0.4，36 元素） | **+0.489** |
| 网格（P0，64 元素） | **+0.952** |
| 自由布局（P7，全部 6 个配置） | **+0.000 ~ +0.073** |

原因很具体：**真实自由布局的精度要求低**。
元素又大又少（sparse 里 60px/192px ≈ 31%），而 H2 已证明 reg 的难度就是精度要求。
精度要求一低，**reg 直接满分**（1.0000），没有留给结构的空间。

### 3.2 解析器代价很大，且在非均匀背景上**崩溃**

| 背景 | parser recall |
|------|---------------|
| flat | 0.845 – 0.905 |
| texture | **0.010** |
| natural | **0.095** |

"四角估计背景 + 阈值"的朴素解析器在**非均匀背景上彻底失效**——
背景本身与元素一样偏离"背景色"，阈值法无法区分。

### 3.3 **`cls-parser` 在全部 6 个配置下都输给 `reg`**

真实自由布局 UI 上，**朴素坐标回归是更好的选择**。

---

## 4. 收敛成一条不等式

把结果整理一下，规律非常干净：

> **`cls-parser` 优于 `reg`  ⟺  `parser_recall > reg_accuracy`**

直觉：`cls-parser` 的成功率 ≈ recall（选择本身几乎完美）；
`reg` 的成功率就是它自己的 acc。所以谁高谁赢。

**验证**：

| 来源 | reg acc | recall | recall > reg? | 结果 | 符合 |
|------|---------|--------|---------------|------|------|
| P0.5 clean | 0.4935 | 1.000 | ✅ | cls 赢 (+0.507) | ✅ |
| P0.5 jit2 | 0.4935 | 1.000 | ✅ | cls 赢 (+0.506) | ✅ |
| P0.5 drop30 | 0.4935 | 0.700 | ✅ | cls 赢 (+0.205) | ✅ |
| P0.5 heavy | 0.4935 | 0.498 | ❌ | cls 输 (−0.022) | ✅ |
| P7 sparse | 1.0000 | 0.905 | ❌ | cls 输 (−0.103) | ✅ |
| P7 dense | 0.9958 | 0.848 | ❌ | cls 输 (−0.162) | ✅ |
| P7 extreme+natural | 0.9258 | 0.095 | ❌ | cls 输 (−0.855) | ✅ |

**10/10 全部符合。** P0.5 当年观察到的 "crossing 在 recall ≈ 0.5" 不是巧合——
那只是因为当时 `reg = 0.4935`，所以交叉点恰好落在 0.5 附近。

**这条不等式统一了 P0 / P0.5 / P7，并且是可操作的**：
选结构化动作之前，先算两件事——**解析器召回**和**回归基线精度**。

---

## 5. 对整体结论的修正（重要）

P7 是本项目**唯一直接检验"是否迁移到真实 UI"的实验**，它的结论是负面的：

> **H2 在真实自由布局 UI 上不成立**（用朴素 CV 解析器时）。

原因不是 H2 的机制错了，而是**它在真实 UI 上缺一个必要条件**：

| 条件 | 网格环境 | 真实自由布局 |
|------|----------|-------------|
| 精度要求高（弱 reg 基线） | ✅ 元素小、数量多 | ❌ 元素大、数量少 |
| 廉价且可靠的解析器 | ✅ oracle / 连通域在纯色上完美 | ❌ 非均匀背景上崩溃 |

**两个条件在真实 UI 上恰好都不满足。**

这也修正了整个项目最核心的叙事：

> 原来我们说"CUA 的瓶颈是必须从高熵输入里恢复哪些信息"。
> P7 给出更精确的说法：**恢复信息的那一步（解析器）本身就是瓶颈**，
> 而它在真实 UI 上不是免费的——**这正是"廉价结构"假设最脆弱的地方**。

### 但这不是对 H2 的否定

H2 的机制（离散选择 vs 连续回归）在**受控条件下**被 10/10 验证。
P7 说明的是：**它的实用价值取决于解析器的质量，而解析器不便宜。**

两者合起来才是完整答案：
- **动作空间压缩是真的**
- **它被解析器成本吃掉了**
- **而解析器成本正是"把结构从像素里恢复出来"的代价** —— 回到了原点

---

## 6. 局限

- 仍是合成环境；自由布局是程序生成的，不是真实截图。
- 只用了**一种**解析器（朴素 CV）。学习式 UI 检测器可能好得多，
  但那要花参数——正是 P0.5 说的"参数只是搬家"的风险。
- `reg` 用的仍是 soft-argmax 坐标回归；更强的回归头（如多假设/热力图金字塔）
  可能进一步压缩 space 效应。
- texture/natural 背景的 recall 崩溃部分源自解析器的朴素设计
  （四角估计背景），不代表所有经典 CV 方法都会崩。
- 未测主假设 vs 大模型（无 API key）。

---

## 7. 下一步

| 优先级 | 事项 |
|--------|------|
| 高 | **学习式 UI 检测器**：测"解析器要多小才能保住 H2"，即 P0.5 的"参数搬家"风险量化 |
| 高 | **真实截图**：P7 已把合成环境推到自由布局+纹理，下一步必须是真图 |
| 中 | 更强的 `reg` 基线（多假设回归），看 space 效应是否进一步消失 |
