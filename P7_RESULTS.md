# P7 结果：真实自由布局 UI 上的迁移检验

> 复现：
> ```
> python p7_realistic_ui.py --n-train 4000 --n-test 1200 --epochs 25
> ```

---

## 1. 动机

REPORT.md §10 的最大局限：**所有结论都建立在网格状、纯色 UI 上**。
真实 UI 是自由摆放、大小不一、有圆角/边框/阴影/渐变、会重叠、背景有纹理。

本实验去掉网格，做**两因子分解**，把"动作空间优势"与"解析器代价"分开：

| 条件 | 测什么 |
|------|--------|
| `reg` | 像素 → (x,y) |
| `cls-oracle` | 真值框 → 选择 → 执行中心（纯动作空间效应） |
| `cls-parser` | 廉价 CV 解析器框 → 选择 → 执行（加上解析器代价） |

**"廉价"的定义**：不含任何学习模型——四角估计背景 + 阈值 + 形态学开运算 + 连通域。

**两个自变量**：
- **布局密度/元素尺寸**（决定 `reg` 的难度）
  sparse(12 大) → medium(20) → dense(30) → extreme(42) → **tiny(42 但极小)** → **micro(48 极小)**
- **背景**：flat / texture / natural

---

## 2. 结果

| config | recall | reg | cls-oracle | cls-parser | **space** | **parser cost** | cls-parser vs reg |
|--------|--------|-----|-----------|-----------|-----------|-----------------|-------------------|
| sparse+flat | 0.905 | 1.0000 | 1.0000 | 0.8967 | +0.000 | +0.103 | −0.103 |
| medium+flat | 0.863 | 1.0000 | 1.0000 | 0.8542 | +0.000 | +0.146 | −0.146 |
| dense+flat | 0.848 | 0.9967 | 1.0000 | 0.8358 | +0.003 | +0.164 | −0.161 |
| extreme+flat | 0.845 | 0.9767 | 1.0000 | 0.8325 | +0.023 | +0.168 | −0.144 |
| **tiny+flat** | 0.847 | **0.7125** | 0.9992 | **0.8392** | +0.287 | +0.160 | **+0.127** |
| **micro+flat** | 0.725 | **0.4967** | 0.9875 | **0.7192** | +0.491 | +0.268 | **+0.223** |
| tiny+texture | **0.026** | 0.6375 | 0.9975 | 0.0117 | +0.360 | +0.986 | **−0.626** |
| tiny+natural | **0.077** | 0.6058 | 0.9950 | 0.0558 | +0.389 | +0.939 | **−0.550** |

（space = cls-oracle − reg；parser cost = cls-oracle − cls-parser）

---

## 3. 四条发现

### 3.1 动作空间优势随**精度要求**增长 —— 与 H2 一致

| 布局 | 元素尺寸/图像 | space 效应 |
|------|-------------|-----------|
| sparse | ~31% | +0.000 |
| dense | ~19% | +0.003 |
| extreme | ~14% | +0.023 |
| **tiny** | **~9%** | **+0.287** |
| **micro** | **~7%** | **+0.491** |

**在自由布局 UI 上同样成立**：元素越小，`reg` 越吃力，结构化选择的价值越大。
这与 P0 在网格环境测到的规律一致（fill 0.75→0.28 时 Δ 0.003→0.622）。

### 3.2 **crossing 出现**：`cls-parser` 在 tiny/micro 上反超

```
extreme+flat   reg=0.9767  cls-parser=0.8325   →  reg 赢  −0.144
tiny+flat      reg=0.7125  cls-parser=0.8392   →  cls 赢  +0.127   ← 交叉
micro+flat     reg=0.4967  cls-parser=0.7192   →  cls 赢  +0.223
```

**这是本项目第一次在非网格、自由布局、带圆角/阴影/渐变的 UI 上
让结构化动作空间胜出。**

### 3.3 解析器在非均匀背景上**崩溃**

| 背景 | recall（tiny 布局） |
|------|-------------------|
| flat | 0.847 |
| texture | **0.026** |
| natural | **0.077** |

"四角估计背景 + 阈值"的朴素解析器在非均匀背景上彻底失效。

### 3.4 因此 `cls-parser` 在纹理/自然背景上仍然大输

即使精度要求很高（space +0.36~+0.39），**解析器 recall 掉到 0.03~0.08**，
`cls-parser` 仍然输给 `reg` −0.55 ~ −0.63。

---

## 4. 统一不等式（12/12 全部符合）

> **`cls-parser` 优于 `reg`  ⟺  `parser_recall > reg_accuracy`**

| 来源 | reg acc | recall | recall > reg? | 结果 | 符合 |
|------|---------|--------|---------------|------|------|
| P0.5 clean | 0.4935 | 1.000 | ✅ | +0.507 | ✅ |
| P0.5 jit2 | 0.4935 | 1.000 | ✅ | +0.506 | ✅ |
| P0.5 drop30 | 0.4935 | 0.700 | ✅ | +0.205 | ✅ |
| P0.5 heavy | 0.4935 | 0.498 | ❌ | −0.022 | ✅ |
| P7 sparse | 1.0000 | 0.905 | ❌ | −0.103 | ✅ |
| P7 dense | 0.9967 | 0.848 | ❌ | −0.161 | ✅ |
| P7 extreme | 0.9767 | 0.845 | ❌ | −0.144 | ✅ |
| **P7 tiny** | **0.7125** | **0.847** | ✅ | **+0.127** | ✅ |
| **P7 micro** | **0.4967** | **0.725** | ✅ | **+0.223** | ✅ |
| P7 tiny+texture | 0.6375 | 0.026 | ❌ | −0.626 | ✅ |
| P7 tiny+natural | 0.6058 | 0.077 | ❌ | −0.550 | ✅ |

直觉：`cls-parser` 的成功率 ≈ recall（选择本身几乎完美）；`reg` 的成功率就是自己的 acc。
**谁高谁赢。**

P0.5 当年观察到的 "crossing 在 recall ≈ 0.5" 不是巧合——
那只是因为当时 `reg = 0.4935`，交叉点恰好落在 0.5 附近。

**这条不等式统一了 P0 / P0.5 / P7，并且可操作**：
选结构化动作之前，先算两件事——**解析器召回**与**回归基线精度**。

---

## 5. 结论（修正）

### 5.1 H2 **可以**在真实自由布局 UI 上成立

在 **`tiny`/`micro`（元素 ~7–9% 图像尺寸）** 且 **flat 背景** 时，
`cls-parser` 反超 `reg`（+0.127 / +0.223）。
这是机制在非网格、带阴影渐变圆角的真实风格 UI 上的首次验证。

### 5.2 但它需要**两个条件同时成立**

| 条件 | 含义 | 何时失败 |
|------|------|----------|
| **精度要求高** | `reg` 的 acc 要低 | 元素大而少（典型 UI）→ reg 直接 1.0 |
| **解析器可靠** | `recall > reg_acc` | 纹理/自然背景 → recall 掉到 0.03 |

**典型真实 UI（大按钮、纯色背景）满足前者但不满足后者？不——是两者都不满足或只满足一个。**

- 大按钮 + 纯色背景：reg 满分 → 不需要结构
- 大按钮 + 纹理背景：reg 仍接近满分 → 不需要结构
- 小图标 + 纯色背景：**结构赢**（tiny/micro）
- 小图标 + 纹理背景：解析器崩 → 结构输

**⇒ 结构化动作空间的实用区间 = `{精度要求高} ∩ {解析器可靠}`。**
真实桌面 UI 里，**小图标 + 干净背景**（工具栏、图标网格）属于这个区间；
**纹理背景 / 照片 / 画布类应用**不在其中。

### 5.3 瓶颈的位置被精确定位

原来我们说"CUA 的瓶颈是必须从高熵输入里恢复哪些信息"。
P7 把它落成一个**可计算的不等式**，并指出：

> **瓶颈是解析器召回 vs 回归精度这两个数的比较。**
> "把结构从像素里恢复出来"这一步的成本，决定了结构化小模型是否划算。

**这正是"小而强"假设最脆弱的地方，也是下一步该攻的地方。**

---

## 6. 局限

- 仍是合成环境；自由布局是程序生成的，不是真实截图。
- 只用了**一种**解析器（朴素 CV）。学习式 UI 检测器可能大幅提高 recall，
  尤其在纹理背景上——但那要花参数，正是 P0.5 说的"参数只是搬家"的风险。
- `reg` 用的是 soft-argmax 坐标回归；更强的回归头（多假设/热力图金字塔）
  可能进一步压缩 space 效应。
- 未测主假设 vs 大模型（无 API key）。

---

## 7. 下一步

| 优先级 | 事项 | 为什么 |
|--------|------|--------|
| 高 | **学习式 UI 检测器** | 直接攻 §3.3 的纹理崩溃：测"解析器要多小才能把 recall 拉过 reg_acc" |
| 高 | **真实截图** | P7 已推到自由布局+纹理；下一步必须是真图 |
| 中 | 更强的 `reg` 基线 | 看 space 效应是否进一步消失 |
