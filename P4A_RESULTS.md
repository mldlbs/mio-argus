# P4a 结果：多样性探针

> 复现：`python p4_population.py --epochs 20`
> 判据见 [ROADMAP.md](ROADMAP.md) §3 P4a

---

## 0. 设计上的一个必要修正

原设想是"10 个模型互相竞争，看谁会做不同的动作"。但实现前就发现：

> **在单步任务上，行为多样性不可能存在** —— 最优动作唯一（找到那个 label 的格子）。
> 所有好策略都会点同一个格子。"选不同动作"是个伪指标。

真正可能的多样性是**失败剖面的差异**：
precision 专精者在细元素上强、粗元素上弱；coarse 专精者反之。
只有这种差异才可能被 verifier / 集成利用。

因此 P4a 测 **coverage**：

```
population coverage@k  = P(真值 ∈ k 个成员 top-1 的并集)
within-policy cover@k  = P(真值 ∈ 单个成员 top-k)
```

两者都用 k 个候选。population 覆盖更高 → 跨策略多样性有价值。

---

## 1. 设置

- grid 固定 6（S=36），保证所有成员动作空间一致
- 专精通过**训练数据切片**实现（只改元素尺寸与本抖动）
- 共同混合测试集：fill ∈ {0.28, 0.40, 0.55, 0.75}
- 5 个成员：`mixed` / `precise` / `coarse` / `jittered` / `mixed(seed=1)`
- 4000 训练样本，20 epochs，width 32（106,112 参数/成员）

---

## 2. 专精确实存在 ✅

成员在共同测试集上的分层准确率：

| 成员 | fill=0.28 | fill=0.40 | fill=0.55 | fill=0.75 | 总体 |
|------|-----------|-----------|-----------|-----------|------|
| mixed | 1.0000 | 1.0000 | 1.0000 | 0.9972 | **0.9993** |
| precise | **1.0000** | 0.9359 | 0.8509 | **0.7654** | 0.8880 |
| coarse | 0.9305 | 0.9610 | 1.0000 | **1.0000** | 0.9733 |
| jittered | 1.0000 | 1.0000 | 1.0000 | 1.0000 | **1.0000** |
| mixed(seed=1) | 1.0000 | 1.0000 | 1.0000 | 0.9972 | 0.9993 |

`precise` 与 `coarse` 呈现**干净的权衡**：
precise 在细元素上 1.0000、粗元素上 0.7654；coarse 正好相反。

错误集 Jaccard 几乎为 0（`mixed vs precise` 0.006，其余多为 0.000），
两两 top-1 分歧率 0.0007–0.138。

**结论：失败剖面的差异是真实存在的，且可以被构造。**

---

## 3. 但 coverage 增益 ≈ 0 ❌

| k | population cover@k | within-mean@k | **within-max@k** | gap(mean) | **gap(max)** |
|---|--------------------|---------------|------------------|-----------|--------------|
| 1 | 0.9720 | 0.9720 | 1.0000 | +0.0000 | **−0.0280** |
| 2 | 0.9998 | 0.9919 | 1.0000 | +0.0079 | **−0.0002** |
| 3 | 1.0000 | 0.9961 | 1.0000 | +0.0039 | **+0.0000** |
| 4 | 1.0000 | 0.9979 | 1.0000 | +0.0021 | **+0.0000** |
| 5 | 1.0000 | 0.9989 | 1.0000 | +0.0011 | **+0.0000** |

关键在最后一列：**最强的单个成员，其 top-2 覆盖就已达 1.0000**。
population 的并集**加不出任何东西**。

而且专精者比通才**更差**：`precise` 总体 0.8880 vs `mixed` 0.9993。
专精的代价是牺牲泛化，而通才本来就能学会两者。

---

## 4. 结论：P4a **未通过判据**

ROADMAP.md §3 写明的判据是：

> `population coverage@k > within-policy coverage@k`（同等 k）。
> 若不成立，population 无价值，P4b/c 免谈。

对照**最强单成员**基线，`gap(max)` 在所有 k 上都 ≤ 0。

**⇒ 在本任务空间里，population 路线不成立。**

### 根因

不是"多样性造不出来"（§2 证明造得出来），而是：

> **这个任务空间不需要专精 —— 单个通才在 1/5 的参数预算下就已饱和。**
> `mixed` 用 106K 参数在全部难度上达到 0.9993（多数层 1.0000）。
> 当通才能覆盖全空间时，任何专精组合都不可能超过它。

这也顺带说明：**参数匹配比较（5×106K vs 一个大通才）也不会有利** ——
大通才只会更饱和。

---

## 5. 本结果的价值

它看起来是负面的，实际上省掉了一整条支线：

1. **否证了"把 N 个模型平均一下就能变强"** —— 在通才可饱和的空间里，集成无增益。
2. **给出了 population 成立的必要条件**：

   > 必须存在一个任务空间，其中**单个策略在给定预算下无法覆盖全部**。
   > 否则专精与集成都是浪费。

3. **这个条件是可以用实验检验的**，见下。

---

## 6. 什么情况下结论会翻转

要让 population 有意义，必须让"通才覆盖不了"成为**可测事实**，而不是假设：

| 途径 | 具体做法 | 为什么可能有效 |
|------|----------|----------------|
| **目标真正冲突** | 不只按难度切片，而是让目标互相排斥（如"最少重观察" vs "最高恢复率"） | 同一策略无法同时最优 |
| **容量硬约束** | 把成员压到极小（如 width 4），使单个成员无法覆盖全部难度 | 强制分工 |
| **任务空间加宽** | 加入互相矛盾的任务族（不同 label 语义、不同布局规则） | 单一策略需容纳冲突 |
| **奖励不对称** | 不同成员面对不同的 verifier 成本（如观察有代价） | 最优策略随成本而变 |

**建议的下一步（P4a'）**：在**固定总参数量**下，比较
「N 个小成员」vs「一个大通才」，并把容量扫到**通才无法饱和**的区间。
只有当通才出现明确的失败区间时，population 才有机会。

若找不到这样的区间 → **population 路线整体放弃**，回到 H1/H2/H3 的单模型路线
（那条路线已有三个实测支撑）。

---

## 7. 局限

- 环境是合成的、单动作类型（点击）。
- 专精只做了"难度切片"一种，未测"目标冲突"（见 §6）。
- 未做参数匹配比较（因通才已饱和，预计无差别，但未实测）。
- 5 个成员是任意选的；但结论（gap(max) ≤ 0）对成员数量不敏感。
