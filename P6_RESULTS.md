# P6 结果：噪声验证器 + V 稳定性修复

> 复现：
> ```
> python p6a_v_stability.py
> python p6_noisy_verifier.py --tasks 200
> ```

---

## 0. 为什么做这个

REPORT.md §10 声明过一个局限：**P3 的进度信号是合成无噪声的，V 达到 acc=1.0**。
真实环境的 verifier 必然有错。本实验回答：

> **验证器要多准？这个要求随 horizon 如何变化？**

---

## 1. 先修一个真实缺陷：V 的训练不稳定

P3 完整运行 3 次中 1 次、P6 quick 运行中 1 次，V 退化为**恒输出正类**
（tpr=1.0, fpr=1.0），导致 π+V 崩到 0。我原本假设是"没学会"。

### 诊断推翻了假设

| seed | AUC | acc@0.5 | **acc@精确最优阈值** |
|------|-----|---------|---------------------|
| 0–4 | **1.000** | 0.51–1.00 | **1.0000** |
| 5 | 0.507 | 0.509 | 0.491 |

**AUC = 1.0 在所有 seed、所有训练配置下都成立**——
V **从来都学到了完美判别器**。真正的病因是：

> `OutcomeVerifier` 末端是无归一化的 Linear，**logit 尺度在训练中剧烈漂移**。
> 实测校准出的最优阈值在 **0.000 到 1.000** 之间乱跳。
> 固定 0.5 必然间歇性崩溃。

我第一版的阈值搜索用 `linspace(0,1,201)` 的均匀网格，
**漏掉了尺度漂移后的最优点**，所以一度误判为"没学到"。
正确做法：最优阈值必落在**排序分数的相邻两点之间**，用相邻中点做候选是精确的。

### 修复

`p3_verifier.py` 新增：
- `calibrate_threshold(v, val_data)` —— 验证集上精确选阈值
- `train_v_calibrated(...)` —— 训练 + 校准 + 低 AUC 重试

**效果**：

| | min test acc | max test acc |
|---|---|---|
| 固定阈值 0.5 | 0.5090 | 0.9960 |
| **校准后** | **1.0000** | **1.0000** |

6/6 seed 全部到 100%，且每个只需 1 次尝试。

**这条独立值得记录**：一个"偶发崩溃"的组件，根因是**阈值不可靠**而非**能力不足**。
诊断靠的是 AUC（阈值无关量）与固定阈值准确率的对比。

---

## 2. P6 主结果：验证器精度 vs horizon

设置：π 单步 acc = **0.9680**；V 校准后 thr=0.9348、AUC=1.0、test acc=1.0；
ρ=1.0（每步布局都变），每配置 200 任务。
**噪声翻转使用独立 rng**，保证不同 ε 跑的是同一批任务。

### 假阴性（FNR）：误拒正确动作

| ε | H=1 | H=2 | H=4 | H=8 |
|---|-----|-----|-----|-----|
| 0.00 | 0.9850 | 0.9850 | 0.9700 | 0.9450 |
| 0.02 | 0.9550 | 0.9250 | 0.8950 | 0.8200 |
| 0.05 | 0.9300 | 0.8550 | 0.8000 | 0.6700 |
| 0.10 | 0.8900 | 0.7950 | 0.6100 | 0.4200 |
| 0.20 | 0.7700 | 0.6700 | 0.4400 | 0.1900 |
| 0.40 | 0.5850 | 0.3300 | 0.0900 | 0.0450 |

### 假阳性（FPR）：误收错误动作

| ε | H=1 | H=2 | H=4 | H=8 |
|---|-----|-----|-----|-----|
| 0.00 | 0.9850 | 0.9850 | 0.9700 | 0.9450 |
| 0.10 | 0.9750 | 0.9850 | 0.9450 | 0.8950 |
| 0.20 | 0.9750 | 0.9800 | 0.9600 | 0.8650 |
| 0.40 | 0.9750 | 0.9600 | 0.9100 | 0.8750 |

### 对称翻转（both）

| ε | H=1 | H=2 | H=4 | H=8 | 半衰 ε |
|---|-----|-----|-----|-----|--------|
| 0.00 | 0.9850 | 0.9850 | 0.9700 | 0.9450 | — |
| 0.02 | 0.9550 | 0.9250 | 0.8950 | 0.8250 | — |
| 0.05 | 0.9300 | 0.8550 | 0.7750 | 0.6700 | — |
| 0.10 | 0.8900 | 0.7900 | 0.6250 | 0.4500 | **H=8** |
| 0.20 | 0.7800 | 0.6400 | 0.4200 | 0.1700 | **H=4** |
| 0.40 | 0.5850 | 0.3250 | 0.1000 | 0.0350 | **H=2** |

---

## 3. 两条结论

### 3.1 **FNR 与 FPR 的代价极不对称** —— 验证器必须偏向"接受"

| | ε=0.40 时的 H=8 成功率 |
|---|---|
| FNR（误拒正确） | **0.0450** |
| FPR（误收错误） | **0.8750** |

**FPR 到 40% 都几乎无害；FNR 到 10% 就已经把 H=8 打到 0.42。**

原因很具体：

- **FNR 会摧毁一个本来确定的成功**——正确动作被拒 → 该步直接失败
- **FPR 只是没能挽救一个本来就要失败的步**——首个候选是错的时候，
  这一步大概率本来就会失败；误收只是让它更早失败

⇒ **可操作的设计规则**：闭环用的 verifier 应当**校准到偏保守**
（宁可放过，不可错杀），把 FNR 压到接近 0，容忍 FPR 高得多。

### 3.2 **可容忍的错误率随 horizon 收紧，近似 ε\* ∝ 1/H**

both 模式的半衰点：

| H | 半衰 ε | H × ε |
|---|--------|-------|
| 2 | 0.40 | 0.80 |
| 4 | 0.20 | 0.80 |
| 8 | 0.10 | 0.80 |

**H 每翻一倍，容错减半**，即 `ε* ≈ 0.8 / H`。

这与 H1 的机制一致：每步的验证错误会**复利**，所以 H 越大容错越低。

**可操作的工程目标**：

| 任务步长 H | 验证器 FNR 要求 |
|-----------|----------------|
| 2 | < 40% |
| 4 | < 20% |
| 8 | < 10% |

---

## 4. 与 H3 的关系

P3 说"结果验证有效"；P6 说"**有效到什么精度为止**"。两者合起来给出 H3 的完整边界：

| 层 | 结论 |
|----|------|
| 形式 | 重新接地 ❌ / 结果验证 ✅ |
| 精度 | FNR 必须 < ~0.8/H；FPR 可放宽到 40% |
| 方向 | 宁可漏检，不可错杀 |

---

## 5. 局限

- 进度信号仍是**合成**的；本实验只模拟了"verifier 判断出错"，
  没有模拟"进度条本身误报/延迟/被遮挡"（后者会让环境语义本身变模糊）。
- 噪声是**独立同分布**的；真实 verifier 的错误往往与画面难度相关（异方差），
  会更集中在难例上。
- V 用宽度 8、6,393 参数；更大或更小的 V 会有不同的错误结构。
- 仍是合成环境、单动作类型；未测主假设 vs 大模型（无 API key）。
