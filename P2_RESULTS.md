# P2 结果：验证能否替代策略精度（H3）

> 本文件是手写的分析笔记（含推导），不是自动生成。
> 复现：`python p2_verifier.py --epochs 25 --v-epochs 15`、`python p2_equiv_check.py`

---

## 0. 先说没做到的部分

主假设的完整检验需要一条**真实大模型基线（配置 A）**：

```
A  大 VLM（GPT-4V / Claude / Gemini）零样本或少样本
```

**本次没有跑。** 原因：当前环境没有可用的 LLM API key
（只有 Mio 的），也未安装 `openai` / `anthropic` 包。
适配器已留好（见 `vlm_adapter.py`），有 key 即可接入。

因此本文件只检验 H3，**不检验主假设中"小模型 vs 10¹⁰ 模型"的部分**。

---

## 1. H3 的检验目标

> **H3**：一个远小于策略的 verifier 能 gate 策略，
> 组合系统 `(π, V)` 优于**等总算力**的单体 `π`。

具体比较（参数量对齐）：

| 系统 | 参数量 |
|------|--------|
| `π(w32)` | 106,112 |
| `π(w32) + V(w32)` | 212,225 |
| `π(w64)` 单体 | 392,448 |

若 `π(w32)+V(w32)` 优于 `π(w64)`，则"拆分出验证"比"放大策略"划算 → H3 成立。

---

## 2. Verifier 的设计

```
V(image, instruction, candidate_cell) → 该动作是否正确的 logit
```

实现：backbone → 指令 query `q` → **RoI-pool 候选格**处的特征 `f_cell`
→ `score = <q, f_cell> / sqrt(C) + b`
训练：正样本 = 目标格；负样本 = 随机格 + **π 的高置信错误格**（困难负样本）。

困难负样本是刻意给 H3 的公平版本：V 必须比 π 更会判断 π 自己犹豫的地方。

---

## 3. 为什么这个设计**在原理上**不可能赢

设 backbone 在格 c 区域的特征均值为 `f_c`，指令 query 为 `q`。则

```
π 的 logit(c)  = 在格 c 区域对  <q, feat(p)>  取平均   = <q, f_c>
V 的 score(c)  = <q,  在格 c 区域对 feat(p) 取平均 >   = <q, f_c>
```

**两者恒等**（RoI-pool 与点积可交换）。

这不是实现细节，而是结论：**当验证必须"重新接地"（判断某个候选动作对不对）时，
验证与选择求解的是同一个问题**，能力上限相同，`(π,V)` 无法超过 `π`。

### 实证验证（`p2_equiv_check.py`）

共享 backbone/query 权重后，在同一批输入上：

```
argmax 一致率        : 0.77
score 最大绝对差     : 0.007153
score 平均绝对差     : 0.000971
```

分数几乎逐位相同（量级 ~0.001，差值 ~0.001，因此 argmax 在近似平局处被打乱）。
**等价性成立。**

---

## 4. 实证结果

### 4.1 策略不完美时（`--quick`，8 epochs）：verifier 有害

| ρ | H | π(w32) | π(w64) | π(w32)+V | V 增益 | vs π(w64) |
|---|---|--------|--------|----------|--------|-----------|
| 1.0 | 1 | 0.8500 | 0.9375 | 0.5625 | **−0.2875** | −0.3750 |
| 1.0 | 2 | 0.8000 | 0.9000 | 0.2375 | **−0.5625** | −0.6625 |
| 1.0 | 4 | 0.5500 | 0.7250 | 0.0625 | **−0.4875** | −0.6625 |

诊断（top-3 重排）：

```
P(正确 ∈ π top-3)        = 0.9980    ← re-rank 的理论上限极高
P(V 选对 | 正确在 top-3) = 0.5972    ← 但 V 不会选
联合                     = 0.5960    < π top-1 = 0.8600
```

### 4.2 策略完美时（完整训练）：无空间

```
单步 π(w32)=1.0000  π(w64)=1.0000  V 单独=1.0000
所有 ρ、H 组合下 π(w32) 与 π(w32)+V 均为 ~1.0000，差异 ≤ 0.0033
```

两个区间都给出同一结论：**verifier 没有独立的增益来源**。

---

## 5. 结论

**H3 在"重新接地的 verifier"这一形式下不成立。**

- 原理层面：`V(c) ≡ π(c)`，验证与选择是同构问题。
- 实证层面：策略弱时 V 拖后腿（0.86→0.60），策略强时无空间（双双饱和）。
- 推论：**H3 不能作为与 H2 独立的第二个收益来源。**
  不能"再加个 verifier"就指望第二次提升，如果 verifier 用的是同一个接地器。

这同时也修正了 HYPOTHESIS.md 里 H3 的表述——原表述把验证当成天然更简单的任务，
但在 CUA 里，"判断这个动作对不对"必须先**找到**这个动作指向的东西，
于是退化成同一个接地问题。

### 什么情况下 H3 才可能成立：**结果验证（outcome verification）**

如果验证检查的是**动作的结果**而不是重新接地，难度就不再等同：

```
act → observe new screen → V(旧屏幕, 动作, 新屏幕) → 状态是否推进了？
```

这需要环境有**可观察的目标状态**（任务是否完成、进度是否前进），
以及一个转移模型。本项目当前的任务只有"点击是否落在目标上"，
没有可观察的进度信号，因此无法检验这种形式。

**这是 H3 唯一还活着的形式，也是后续要补的实验。**

---

## 6. 局限

- 未跑配置 A（真实大模型基线），主假设的核心比较仍未测。
- 环境是合成的、单动作类型（点击）。
- 4.1 的"策略不完美"区间来自欠训练（8 epochs），不是"内在困难"造成的错误；
  未能构造出"策略犯有结构性的、可被验证捕获的错误"的场景。
- 结论依赖 `V` 与 `π` 共享同一 backbone 与同一观测。
  若 V 有**额外观测**（如动作后屏幕），结论不再适用——正是第 5 节的方向。
